name: "ClamAV Antivirus"
description: "ClamAV-based antivirus scanner for Home Assistant with web GUI, scheduled scans, quarantine and HA notifications"
version: "1.0.2"
url: "https://github.com/gregorwolf1973/clamav-hassio-addon"
slug: "clamav"
init: false
arch:
  - aarch64
  - amd64
  - armv7
startup: services
boot: manual
ingress: true
ingress_port: 8200
panel_icon: "mdi:shield-bug"
panel_title: "ClamAV"
host_network: false
privileged:
  - DAC_READ_SEARCH
apparmor: false
options:
  scan_paths:
    - /share
    - /media
  scan_schedule: "daily"
  scan_hour: 2
  auto_quarantine: true
  max_file_size_mb: 100
  notify_ha: true
  log_level: "info"
  web_port: 8200
  admin_password_enabled: false
  admin_username: "admin"
  admin_password: ""
schema:
  scan_paths:
    - str
  scan_schedule: list(disabled|hourly|daily|weekly)
  scan_hour: int(0,23)
  auto_quarantine: bool
  max_file_size_mb: int(1,2048)
  notify_ha: bool
  log_level: list(trace|debug|info|notice|warning|error|fatal)
  web_port: port
  admin_password_enabled: bool
  admin_username: str
  admin_password: str
map:
  - share:rw
  - media:rw
  - config:rw
  - all_addon_configs:ro
